# 2026-06-25

## 每日arXiv研究前沿自动化执行
- 生成报告: `arxiv-daily-2026-06-25.md`
- 量子57篇、math.CT 4篇、math.OA 6篇、cs.AI 70篇，合计137篇
- 量子亮点: Furusawa组皮秒猫态(2606.24002)、P-mon高保真门(2606.24772)
- Topos亮点: Mahadevan Infinitesimal Causality (2606.24621) — 范畴论+因果推断交叉
- AI亮点: Agent系统12篇、Bareinboim因果RL综述(2606.24160)

## [SKILL] 微信公众号文章发布 — 坑点复盘

实战文章《复数化的概率》（145个LaTeX公式）→ 微信公众号发布全流程踩坑记录：

**成功路径**（5步）:
1. Markdown → HTML 转换（保留公式 `\(...\)` / `\[...\]`）
2. LaTeX 公式 → PNG base64 内嵌（用 MathJax + Playwright/Sharp 渲染）
3. 封面图单独准备（AI配图或手动设计）
4. HTML 粘贴到微信编辑器
5. 手机预览确认 → 保存草稿

**失败尝试**:
1. SVG 内联方案 → ❌ 微信过滤 `<svg>` 标签，手机端显示原始 LaTeX 代码
2. MathJax CDN 方案 → ❌ 微信剥离外部 `<script>` 标签
3. `npx serve` 本地预览 → ⚠️ PowerShell 下卡住（首次下载依赖无进度提示）
4. Playwright + Chromium → ⚠️ 可行但太重（~300MB），推荐改用 sharp

**环境依赖**:
- wenyan-cli: `npm install -g @wenyan-md/cli`（最推荐，自动化发布）
- 微信 API 凭证: AppID + AppSecret + IP白名单
- Playwright: `npm install playwright && npx playwright install chromium`
- sharp（推荐替代 Playwright）: `npm install sharp`
- 文章配图目录: `wexin/C01-复数化的概率/公众号配图/`

**更新位置**: `C:\Users\RR\.codebuddy\skills-marketplace\skills\wechat-article-pro\SKILL.md` (v1.0.0 → v1.1.0)

## [SKILL] 微信公众号公式渲染成功 — Playwright 批量方案

成功将《复数化的概率》145个公式渲染为 PNG base64：

**最终方案**: Playwright + MathJax CDN 批量渲染（render-png-batch.cjs）
- 一次页面加载渲染所有公式（145个占位符 → 93个唯一公式，去重率36%）
- 输出文件 289KB（公众号文章-复数化的概率-wx-png.html）
- 所有占位符 0 残留，0 fallback，145个 PNG 全部成功

**新增坑点记录**:
- 坑7: mathjax-full 3.x 在 Node v26 CJS 下不可用（ESM import 兼容问题）
- 坑8: Playwright 批量渲染优化（一次加载 + waitForFunction + 缓存去重）
- 坑9: Chromium 下载可能因网络波动失败（自动重试成功）

**skill 更新**: `c:\Dev\.codebuddy\skills\weixin.md` 新增坑7-9 + 成功路径速查
