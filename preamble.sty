\PassOptionsToPackage{final}{hyperref}

\usepackage{comment}
\usepackage{silence}
% 已知无害的包间命令重定义警告 — 必须在 sectsty/hyperref 之前
\WarningFilter{LaTeX}{has changed}
% 共享 preamble 中 glossaries 可能在个别文档中未使用
\WarningFilter{glossaries}{No \\printglossary}

% ============== 考虑出血的版式 ===================================
\usepackage[
    paperwidth=6in,
    paperheight=9in,
    top=0.8in,
    bottom=0.8in,
    inner=0.75in,
    outer=0.6in,
    bindingoffset=0.25in,
    footskip=0.4in
]{geometry}

% ============== 语言和字体 ===================================
\usepackage{hyperref}
\usepackage{fontspec}
\usepackage[english]{babel}
\usepackage{sectsty}

% 正文: NotoSerifSC (支持中文+西文)
% 注意: 不使用 HarfBuzz 渲染器，因为它在 CJK 字符映射时有 BUG，
% 会将部分标准 CJK 字符映射到 Kangxi Radical/CJK Compatibility 码位
\babelfont{rm}[
    Path = ./font/,
    BoldFont = {NotoSerifSC-ExtraBold.ttf},
    ItalicFont = {StKaiTi.ttf}
]{NotoSerifSC-Regular.ttf}

\babelfont{sf}[Path = ./font/]{GFSDidot-Regular.ttf}
\babelfont{tt}[Path = ./font/]{NotoSerifSC-Regular.ttf}

\allsectionsfont{\rmfamily\bfseries}

\usepackage{amsmath, amssymb, amsthm}
\usepackage{amscd} 
\usepackage{mathtools}

\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{quoting}
\usepackage{setspace}
\onehalfspacing

\usepackage{tikz}
\usepackage{tikz-cd}
\usetikzlibrary{decorations.pathmorphing, decorations.markings}

\usepackage{fancyhdr}
\usepackage{bm} 
\usepackage[libertine]{newtxmath} 
\usepackage{nicematrix} 
\usepackage{adjustbox} 
\usepackage{framed} 
\usepackage{scrextend}
\WarningFilter{latexfont}{Font shape}
\WarningFilter{latexfont}{Size substitutions}
\WarningFilter{latexfont}{Some font shapes}
\WarningFilter{hyperref}{unreferenced destination}

\usepackage{physics}

\usepackage[toc,page]{appendix}
\changefontsizes{9.5pt} 

\NiceMatrixOptions{
  custom-line = {
    letter = : ,
    command = hdashline ,
    tikz = { dashed }
  }
}

\setlength{\parskip}{1em} 

\newtheorem{example}{例}[chapter]

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}

%\usepackage{cite}
\usepackage[style=index]{glossaries}   % 生成类似于索引的格式
\makeglossaries

\usepackage{makeidx}
\makeindex

\usepackage{csquotes}
\DeclareQuoteStyle[american]{chinese}
  {\symbol{"201C}}{\symbol{"201D}}
  {\symbol{"2018}}{\symbol{"2019}}
\usepackage[backend=biber, style=authoryear-comp, sorting=nyt, citestyle=authoryear, language=english, autolang=none]{biblatex}
% 引用格式 [Author Year] 方括号
\renewcommand*{\mkbibparens}[1]{#1}
\DeclareCiteCommand{\cite}[\mkbibbrackets]
  {\usebibmacro{prenote}}
  {\usebibmacro{citeindex}%
   \usebibmacro{cite}}
  {\multicitedelim}
  {\usebibmacro{postnote}}
